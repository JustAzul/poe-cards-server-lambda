/**
 * Presentation metadata from poe.ninja item lines
 * Stays in infrastructure layer — not part of domain model
 */
export interface PoeNinjaItemMeta {
  artFilename?: string;
  flavourText?: string;
}
